# linux-utils

Collection of small Linux / POSIX utilities written in C while I learn:

- getopt and getopt_long
- system calls (open, read, write, lseek)
- file I/O behavior
- error handling with errno
- memory layout, structs, pointer exercises

Each program lives in the **src/** directory.
